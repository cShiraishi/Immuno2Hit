# Vercel deploys ONLY the static frontend. Everything Python lives in backend/, which is
# hidden here as a second line of defence: with no requirements.txt and no entrypoint
# filename at the root, Vercel's Python runtime detection cannot fire at all.
backend/
Dockerfile
*.command
README.md
