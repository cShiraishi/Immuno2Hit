# Dependencies for the Vercel Functions in api/. Kept minimal on purpose: the bundle
# limit is 500 MB and these come to ~367 MB installed on Linux.
#
# No pandas here (39.8 MB) — only backend/predict.py, the offline CLI, needs DataFrames.
# xgboost-cpu instead of xgboost: the normal Linux wheel declares nvidia-nccl-cu12,
# 303 MB of GPU libraries that CPU inference never touches.
numpy>=1.26
scikit-learn==1.6.1
rdkit>=2024.3
xgboost-cpu>=3.0; platform_system == "Linux"
xgboost>=3.0; platform_system != "Linux"
