# Dependencies for the Vercel Functions in api/. ~251 MB installed on Linux, against a
# 500 MB function limit — and Vercel also bundles the .pyc it compiles, so headroom matters.
#
# No RDKit (148 MB): the browser computes fingerprints with RDKit.js, verified bit-for-bit
# identical to RDKit Python. No pandas (39.8 MB): only the offline CLI needs DataFrames.
# xgboost-cpu rather than xgboost: the Linux wheel drags in nvidia-nccl-cu12, 303 MB of
# GPU libraries that CPU inference never touches.
numpy>=1.26
scikit-learn==1.6.1
xgboost-cpu>=3.0; platform_system == "Linux"
xgboost>=3.0; platform_system != "Linux"
